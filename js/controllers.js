'use strict';

var lblControllers = angular.module('lblControllers', [])
.controller('homeCtrl', ['$scope', '$window',
  function($scope, $window) {
    // do something
    /*
    var finalistLikes = {};
    for (i = 0; i < finalistUrls.length; i++) {
	      
    }
    return finalistLikes;
    */
    return false; // for now  
  }
]);
